# 西洋占星出生資料精度 KB 文章

此資料夾用於整理 birth time、birth place、Moon、Asc/Desc、houses 與 overlays 的精度護欄。

V0 規則：
- 出生城市可選填，但缺城市時只能用 location fallback。
- 缺出生時間時，Moon 需要降權；Asc、Desc、houses、overlays 不可展示。
- 有出生時間但缺可靠地點時，行星度數仍可讀，但宮位與角度點需要 blocked 或 not available。
